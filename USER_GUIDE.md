# 사용 가이드 📖

바이낸스 카피트레이딩 시스템 초보자 가이드

## 🚀 처음 시작하기

### 1단계: .env 파일 설정 (필수!)

GUI 런처를 실행하면 `.env` 파일이 자동으로 생성되고 메모장이 열립니다.

**최소한 이것만 입력하세요:**

```env
# Testnet API (무료, 실제 돈 사용 안함!)
BINANCE_TESTNET_API_KEY=여기에_Testnet_API_키
BINANCE_TESTNET_API_SECRET=여기에_Testnet_시크릿_키
```

#### Testnet API 키 발급 방법:
1. https://testnet.binancefuture.com 접속
2. GitHub 계정으로 로그인
3. 상단 메뉴 → **"API Management"**
4. **"Generate HMAC_SHA256 Key"** 클릭
5. 생성된 키를 복사해서 .env 파일에 붙여넣기

**⚠️ 중요: Testnet은 가짜 돈으로 테스트하는 환경입니다. 실제 돈이 들어가지 않습니다!**

### 2단계: 모드 선택

GUI 런처에서 두 가지 모드 중 하나를 선택합니다.

---

## 🌐 모드 1: API 서버 (웹 대시보드)

### 이게 뭔가요?

웹 브라우저에서 실시간으로 거래를 **보고** **제어**할 수 있는 모드입니다.

### 언제 사용하나요?

- ✅ **처음 사용할 때** (강력 추천!)
- ✅ 실시간으로 상황을 모니터링하고 싶을 때
- ✅ 수동으로 거래를 제어하고 싶을 때
- ✅ 시스템이 제대로 작동하는지 확인할 때

### 어떻게 사용하나요?

1. GUI 런처에서 **"🌐 API 서버 (웹 대시보드)"** 선택
2. **"▶️ 시작"** 버튼 클릭
3. 웹 브라우저 열기: **http://localhost:8000**

### 웹 대시보드에서 볼 수 있는 것:

```
┌─────────────────────────────────────────┐
│  바이낸스 카피트레이딩 대시보드         │
├─────────────────────────────────────────┤
│  💰 계좌 정보                            │
│     총 자산: $10,000                     │
│     수익률: +5.2%                        │
│     일일 수익: +$520                     │
├─────────────────────────────────────────┤
│  📊 활성 포지션 (현재 거래 중)          │
│     BTCUSDT  롱  +2.3%  $1,000          │
│     ETHUSDT  숏  -0.5%  $500            │
├─────────────────────────────────────────┤
│  🎯 거래 신호                            │
│     BTCUSDT  롱  3명 합의  ⏳ 대기      │
│     SOLUSDT  숏  4명 합의  ✅ 진입      │
├─────────────────────────────────────────┤
│  🔴 긴급 중지  🔄 새로고침              │
└─────────────────────────────────────────┘
```

### 할 수 있는 것:

- ✅ 실시간으로 모든 거래 확인
- ✅ 포지션 수동으로 닫기
- ✅ 긴급 중지 버튼 (모든 거래 즉시 정리)
- ✅ 설정 변경
- ✅ 로그 확인

---

## 🤖 모드 2: 자동 거래 모드

### 이게 뭔가요?

백그라운드에서 **자동으로** 신호를 감지하고 거래를 실행하는 모드입니다.
웹 대시보드 없이 실행됩니다.

### 언제 사용하나요?

- ✅ 24시간 자동 운영하고 싶을 때
- ✅ 서버에서 계속 돌려놓고 싶을 때
- ✅ 충분히 테스트를 마친 후
- ⚠️ **처음 사용자는 이 모드를 사용하지 마세요!**

### 어떻게 사용하나요?

1. GUI 런처에서 **"🤖 자동 거래 모드"** 선택
2. **"▶️ 시작"** 버튼 클릭
3. 로그 창에서 진행 상황 확인

### 어떻게 작동하나요?

```
1. 리더보드 스크래핑 (60초마다)
   ↓
2. 상위 15명 트레이더 포지션 수집
   ↓
3. 신호 분석 (3명 이상 합의 시)
   ↓
4. 리스크 체크 (손실 한도, 포지션 수 등)
   ↓
5. 자동으로 거래 실행
   ↓
6. 포지션 모니터링 (손익 추적)
   ↓
7. 자동으로 포지션 청산 (목표가/손절가)
```

### 로그 확인:

- GUI 런처 하단의 로그 창
- 또는 `logs/` 폴더의 파일

---

## 📋 두 모드 비교표

| 기능 | API 서버 모드 | 자동 거래 모드 |
|------|--------------|---------------|
| 웹 대시보드 | ✅ 있음 | ❌ 없음 |
| 실시간 모니터링 | ✅ 가능 | 📄 로그로만 |
| 수동 제어 | ✅ 가능 | ❌ 불가능 |
| 긴급 중지 | 🌐 웹에서 클릭 | ⏹️ GUI 중지 버튼 |
| 추천 대상 | 초보자, 테스트 | 숙련자, 장기 운영 |
| CPU 사용량 | 약간 높음 | 낮음 |

---

## 💡 초보자 추천 순서

### 1주차: 학습 및 테스트

```
1. Testnet API 발급
2. .env 파일에 키 입력
3. API 서버 모드로 시작
4. 웹 대시보드로 모니터링
5. 어떻게 작동하는지 관찰
```

### 2주차: 파라미터 조정

```
1. config.yaml 설정 변경
2. 리스크 설정 조정
3. 신호 조건 조정
4. 결과 확인
```

### 3주차: 실전 준비

```
1. Testnet에서 최소 2주 테스트
2. 수익률 확인
3. 설정 최적화
4. 자동 거래 모드로 전환
```

### 4주차 이후: 실전 운영 (선택사항)

```
⚠️ 주의: 실제 돈으로 운영하려면
1. .env에 실제 Binance API 키 입력
2. config.yaml에서 testnet_mode: false로 변경
3. 소액부터 시작
4. 절대로 감당할 수 없는 금액을 투자하지 마세요!
```

---

## ⚙️ config.yaml 주요 설정

### 시스템 설정

```yaml
system:
  testnet_mode: true        # true: Testnet, false: 실전
  scrape_interval: 60       # 리더보드 체크 주기 (초)
```

### 전략 설정

```yaml
strategy:
  min_traders_agreement: 3  # 최소 몇 명이 합의해야 신호 발생?
  signal_timeframe: 5       # 몇 분 이내 포지션을 합의로 봄?
```

### 리스크 관리

```yaml
risk:
  max_daily_loss_percent: 5      # 하루 최대 손실 (%)
  max_positions: 5               # 동시 포지션 개수
  position_size_percent: 5       # 포지션당 자본 비율 (%)
  stop_loss_percent: 3           # 손절 (%)
  take_profit_percent: 10        # 익절 (%)
```

### 거래 설정

```yaml
trading:
  leverage: 5                    # 레버리지 배수
  order_type: "MARKET"           # 주문 타입
```

---

## 🔔 알림 설정 (선택사항)

텔레그램으로 알림을 받고 싶다면:

### 1. 텔레그램 봇 생성

1. 텔레그램에서 `@BotFather` 검색
2. `/newbot` 명령어 입력
3. 봇 이름 입력
4. 생성된 **토큰** 복사

### 2. Chat ID 확인

1. 봇과 대화 시작
2. 아무 메시지나 전송
3. 브라우저에서 접속:
   ```
   https://api.telegram.org/bot토큰/getUpdates
   ```
4. "chat":{"id": 숫자} 부분 복사

### 3. .env 파일에 추가

```env
TELEGRAM_BOT_TOKEN=여기에_봇_토큰
TELEGRAM_CHAT_ID=여기에_채팅_ID
```

이제 거래가 발생하면 텔레그램으로 알림이 옵니다!

---

## 🆘 자주 묻는 질문

### Q: Testnet에서 돈이 실제로 빠져나가나요?

**A:** 절대 아닙니다! Testnet은 가짜 돈으로 연습하는 환경입니다.

### Q: API 서버 모드와 자동 거래 모드를 동시에 실행할 수 있나요?

**A:** 아니요. 둘 중 하나만 선택해야 합니다.

### Q: 웹 대시보드가 안 열려요.

**A:**
1. 시스템이 완전히 시작될 때까지 10-20초 기다리세요
2. 브라우저에서 http://localhost:8000 직접 입력
3. 포트 충돌이면 config.yaml에서 포트 변경

### Q: 리더보드 스크래핑이 실패해요.

**A:**
1. Playwright 브라우저 설치: `playwright install chromium`
2. 인터넷 연결 확인
3. Binance 웹사이트 접속 가능 여부 확인

### Q: 신호가 발생하지 않아요.

**A:**
1. `min_traders_agreement` 값이 너무 높은지 확인 (3이 적당)
2. 리더보드에 활성 트레이더가 있는지 확인
3. 로그에서 스크래핑 결과 확인

### Q: 실전으로 전환하려면?

**A:**
1. Testnet에서 최소 2주 이상 테스트
2. .env에 실제 Binance API 키 입력
3. config.yaml의 `testnet_mode: false`로 변경
4. **소액부터 시작!**

---

## 🎯 다음 단계

1. **지금 바로 시작**: `run_gui.bat` 실행
2. **.env 파일 설정**: Testnet API 키 입력
3. **API 서버 모드 선택**: 처음엔 이것부터!
4. **웹 대시보드 확인**: http://localhost:8000
5. **관찰하기**: 어떻게 작동하는지 지켜보기

---

**궁금한 점이 있으면 언제든지 물어보세요!** 😊
